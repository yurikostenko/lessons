// Сучасний (Строгий) режим
"use strict"

/*---------------------------*/

// Задача №1
// Що потрапить в консоль ?

let someVar = 0;
++someVar;

if (someVar) {
   console.log(someVar);
}

//Відповідь:
// 1

/*-------------------------------*/

// Задача №2
// За допомогою циклу FOR виведіть в консоль 10 рядків:
// Пункт №1
// Пункт №2
// і т.д.

let number = `Нас десять`
for (let i = 0; i < number.length; ++i) {
   console.log(`Пункт №${i + 1}`);
}
//Працює, бо "Нас десять" має 10 символів, тобто number.length === 10.
//Але: цей спосіб залежить від кількості символів у рядку, що небезпечно (саме за умовою задачі) — зміниш текст, і цикл зламається.

for (let i = 0; i < 10; ++i) {
   console.log(`Пункт №${i + 1}`);
}
//Найбільш класичний та надійний варіант.

for (let i = 1; i <= 10; ++i) {
   console.log(`Пункт №${i}`);
}
// Теж дуже зручно — одразу виводиться Пункт №1 до Пункт №10 без додавання +1.
// Трохи більш «людяний» у логіці.

for (let i = 0; i < 10;) {
   ++i
   console.log(`Пункт №${i}`)
}
// Працює. Незвична форма, але формально правильна.
// Іноді таке пишуть, коли інкремент залежить від умов усередині циклу.

/*-------------------------------*/

// Задача №3
// Що потрапить в консоль ?

if (2 * 20 <= 10 || 30 / 2 < 5 && 10 <= "10" || 20 === "20") {
   console.log('000');
}

//Відповідь:

// Нічого не потрапить, бо:

// 1. (40 <= 10 || 15 < 5 && 10 <= "10" || 20 === "20")
// 2. (false || false && true || false)
// 3. (false || false || false)
// 4. Результат: false, console.log не виконається

/*----------------------------------*/

// Задача №4
// Створіть функцію, яка повертає результат ділення числа a на число b з додаванням рядка "Результат ділення: "
// Викличте функцію передаючі різні значення, у тому числі не передаючи зовсім.
// Функція не має повертати NaN, Infinite або помилку 

function safeDivide(a, b) {
   const result = a / b;

   if (!Number.isFinite(result)) {
      return 'Результат ділення: недійсні дані';
   }

   return 'Результат ділення: ' + result;
}


// Приклади викликів:
console.log(safeDivide(10, 2));         // Результат ділення: 5
console.log(safeDivide(5, 0));          // Результат ділення: недійсні дані
console.log(safeDivide());              // Результат ділення: недійсні дані
console.log(safeDivide(9));             // Результат ділення: недійсні дані
console.log(safeDivide("6", 2));        // Результат ділення: 3
console.log(safeDivide(NaN, 2));        // Результат ділення: недійсні дані
console.log(safeDivide(Infinity, 2));   // Результат ділення: недійсні дані
console.log(safeDivide(0, Infinity));   // Результат ділення: 0

/*--------------------------------*/

// Задача №5
// Створіть масив даних - 5 елементів, один з яких число 10
// Обробіть масив за допомогою методу перебору
// Перевіряйте елемент на відповідність числу 10, та у разі відповідності, виводьте в консоль

let someArray = [true, ``, `Yurii`, 10, 100]
someArray.forEach((item) => {
   if (item === 10) {
      console.log(item)
   }
})

